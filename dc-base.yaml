version: "3.8"
name: "dev-environment"
services:
  prometheus:
    image: prom/prometheus:${PROMETHEUS}
    build:
      context: .
      dockerfile: deploy/prometheus.dockerfile
    container_name: dev-environment-prometheus.go.tmpl-1
    ports:
      - "9090:9090"
    expose:
      - 9090
    volumes:
      - ./configs/prometheus.go.tmpl/prometheus_cfg.yaml:/etc/prometheus.go.tmpl/cfg.yaml
    command:
      - "--storage.tsdb.retention.time=30d"
      - "--config.file=/etc/prometheus.go.tmpl/cfg.yaml"
    develop:
      watch:
        - action: rebuild
          path: configs/prometheus.go.tmpl/
          target: etc/prometheus.go.tmpl/cfg.yaml
    networks:
      - dev-environment

networks:
  dev-environment:
    driver: bridge